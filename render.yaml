services:
  - type: web
    name: cashpot-backend
    env: node
    plan: free
    buildCommand: npm install && cd client && npm install && npm run build && cd ..
    startCommand: node setup-render-db.js && node real-server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        fromDatabase:
          name: cashpot-gaming-db
          property: connectionString

databases:
  - name: cashpot-gaming-db
    plan: free
    region: oregon 